// Aquí van los getElementById y los querySelectorAll que pondrías al principio.
const gameBoardElement = document.getElementById('game-board');
const formWordElement = document.getElementById('word-form');
const lengthErrorElement = document.getElementById('length-error');
const correctWordElement = document.getElementById('correct-word');
const buttonElement = document.getElementById('button');
const inputElement = document.getElementById('input');

export {
	gameBoardElement,
	formWordElement,
	lengthErrorElement,
	correctWordElement,
	buttonElement,
	inputElement
};
